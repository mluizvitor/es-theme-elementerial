<!--
  Theme: Elementerial
  Author: mluizvitor
-->

<theme>
  <formatVersion>7</formatVersion>

  <view name="system, basic, detailed">
    <helpsystem name="help">
      <textColor>${backgroundDarkColor}</textColor>
      <iconColor>${mainColor}</iconColor>
    </helpsystem>
  </view>

  <view name="basic, detailed">
    <text name="system_name">
      <color>${backgroundDarkColor}</color>
    </text>

    <image name="background">
      <color>${backgroundLightColor}</color>
    </image>

    <image name="cover_list_fallback" extra="true">
      <color>${backgroundLightColor}</color>
    </image>

    <textlist name="gamelist">
      <selectedColor>${onMainColor}</selectedColor>
      <selectorColor>${mainColor}</selectorColor>
      <primaryColor>${backgroundDarkColor}</primaryColor>
      <secondaryColor>${backgroundDarkColor}</secondaryColor>
    </textlist>

    <rating name="md_rating">
      <color>${mainColor}</color>
    </rating>
  </view>

  <view name="detailed">
    <rating name="md_rating">
      <unfilledColor>${mainColor}</unfilledColor>
    </rating>
  </view>

  <view name="menu">
    <menuText name="menutext">
      <selectedColor>${onMainColor}</selectedColor>
      <selectorColor>${mainColor}</selectorColor>
      <color>${backgroundDarkColor}</color>
      <separatorColor>${backgroundDarkColor}0f</separatorColor>
    </menuText>

    <menuTextSmall name="menutextsmall">
      <color>${sectionLightColor}</color>
      <separatorColor>${mainColor}</separatorColor>
    </menuTextSmall>

    <menuGroup name="menugroup">
      <color>${sectionLightColor}</color>
      <separatorColor>${sectionLightColor}80</separatorColor>
      <backgroundColor>${sectionLightColor}0f</backgroundColor>
    </menuGroup>

    <menuBackground name="menubg">
      <color>${backgroundLightColor}</color>
    </menuBackground>

    <menuText name="menufooter">
      <color>${backgroundDarkColor}cc</color>
    </menuText>

    <menuText name="menutitle">
      <color>${backgroundDarkColor}</color>
    </menuText>

    <menuTextEdit name="menutextedit">
      <inactive>./assets/input_inactive.svg</inactive>
      <active>./assets//input_active.svg</active>
    </menuTextEdit>
  </view>

  <view name="system">
    <carousel name="systemcarousel">
      <color>${backgroundLightColor}1a</color>
      <colorEnd>${backgroundLightColor}cc</colorEnd>
    </carousel>

    <!-- System Name -->
    <text name="systemName" extra="true">
      <color>${backgroundDarkColor}</color>
    </text>

    <!-- Total games per system -->
    <text name="systemInfo">
      <color>${backgroundDarkColor}cc</color>
    </text>

    <!-- Logo Fallback -->
    <text name="logoText" extra="true">
      <color>${sectionLightColor}</color>
    </text>

    <!-- System Background -->
    <image name="cover_fallback" extra="true">
      <color>${backgroundLightColor}</color>
    </image>

    <!-- System Background -->
    <image name="osdBackground" extra="true">
      <color>${backgroundLightColor}</color>
    </image>
  </view>

  <!-- Clock Settings -->
  <view name="screen">
    <text name="clock">
      <color>${backgroundDarkColor}</color>
    </text>

    <batteryIndicator name="batteryIndicator">
      <color>${backgroundDarkColor}</color>
    </batteryIndicator>

    <controllerActivity name="controllerActivity">
      <color>${backgroundDarkColor}40</color>
      <activityColor>${mainColor}80</activityColor>
      <hotkeyColor>${mainColor}</hotkeyColor>
    </controllerActivity>
  </view>
</theme>